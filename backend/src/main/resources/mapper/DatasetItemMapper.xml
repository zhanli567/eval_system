<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.agentnexus.backend.dataset.mapper.DatasetItemMapper">
  <insert id="insertBatch">
    INSERT INTO t_eval_dataset_item (
      id,
      space_id,
      version_id,
      row_no,
      created_by_name,
      created_by,
      last_updated_by,
      last_updated_by_name,
      last_updated_date
    )
    VALUES
    <foreach collection="items" item="item" separator=",">
      (
        #{item.id},
        #{item.spaceId},
        #{item.versionId},
        #{item.rowNo},
        #{item.createdByName},
        #{item.createdBy},
        #{item.lastUpdatedBy},
        #{item.lastUpdatedByName},
        #{item.lastUpdatedDate}
      )
    </foreach>
  </insert>
</mapper>
