agentnexus:
  master:
    service:
      url:

server:
  port: ${SERVER_PORT:18080}

remoteCall:
  mock:
    enabled: true
  agent-chat-url:
  connect-timeout-ms: 500000
  read-timeout-ms: 6000000
  iam:
    url:
